// This code is implicitly handled by p5play's `kb` object but we can document the mapping here.
// Makey Makey -> Keyboard keys
// Red (Tomato) -> LEFT ARROW ('ArrowLeft')
// Green (Lettuce) -> UP ARROW ('ArrowUp')
// Yellow (Cheese) -> DOWN ARROW ('ArrowDown')
// Brown (Patty) -> RIGHT ARROW ('ArrowRight')
// Space -> Reset / Start

function setupInputs() {
    // We can define aliases if needed, but p5play's default checks are fine.
    // We will just use kb.presses('left') etc in the BurgerBuilder.
}
